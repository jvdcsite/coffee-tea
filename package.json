{
  "name": "coffee-tea-site",
  "private": true,
  "description": "Vanilla-JS coffee & tea site on a Cloudflare-only free-tier stack. No build step; these scripts only wrap deployment and local dev, they don't compile or bundle anything.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy",
    "db:init:local": "wrangler d1 execute coffee-tea-db --file=./db/schema.sql",
    "db:init:remote": "wrangler d1 execute coffee-tea-db --file=./db/schema.sql --remote"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "wrangler": "^4.0.0"
  }
}
